DAY: 20
DATE: 27.06.2026
LEETCODE QUESTION NO.: 345
LANGUAGE: JAVA

QUESTION:
Given a string s, reverse only all the vowels in the string and return it.
The vowels are 'a', 'e', 'i', 'o', and 'u', and they can appear in both lower and upper cases, more than once.

Example 1:
Input: s = "IceCreAm"
Output: "AceCreIm"

Explanation:
The vowels in s are ['I', 'e', 'e', 'A']. On reversing the vowels, s becomes "AceCreIm".

Example 2:
Input: s = "leetcode"
Output: "leotcede"

SOURCE CODE:
class Solution {
    public String reverseVowels(String s) {
        HashSet<Character> set = new HashSet<>();
        set.add('U');
        set.add('O');
        set.add('I');
        set.add('E');
        set.add('A');
        set.add('u');
        set.add('o');
        set.add('i');
        set.add('e');
        set.add('a');
        int low = 0;
        int high = s.length()-1;
        char[] arr = s.toCharArray();
        while(low<=high){
            if(set.contains(arr[low]) && set.contains(arr[high])){
                char temp = arr[low];
                arr[low] = arr[high];
                arr[high] = temp;
                low++;
                high--;
            } 
            else if(set.contains(arr[low]) && (set.contains(arr[high])!=true)){
                high--;
            }
            else{
                low++;
            }
            
        }
        return String.valueOf(arr);
    }
}
