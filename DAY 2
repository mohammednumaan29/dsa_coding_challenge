DAY: 2
DATE: 09.06.2026

Leetcode Problem No.: 26
Language: Java 

Given an integer array nums sorted in non-decreasing order, remove the duplicates in-place such that each unique element appears only once. The relative order of the elements should be kept the same.
Consider the number of unique elements in nums to be k​​​​​​​​​​​​​​. After removing duplicates, return the number of unique elements k.
The first k elements of nums should contain the unique numbers in sorted order. The remaining elements beyond index k - 1 can be ignored.

Example:

Input: nums = [0,0,1,1,1,2,2,3,3,4]
Output: 5, nums = [0,1,2,3,4,_,_,_,_,_]
Explanation: Your function should return k = 5, with the first five elements of nums being 0, 1, 2, 3, and 4 respectively.
It does not matter what you leave beyond the returned k (hence they are underscores).

Source Code:
class Solution {
    public int removeDuplicates(int[] nums) {
        int k=1;
        for(int i = 0; i < nums.length-1; i++){
            if(nums[i]!=nums[i+1]){
                nums[k] = nums[i+1];
                k++;
            }
        }
        return k;
    }
}

Issues Faced:
Didn't understand the output to be returned correctly and was trying irrelevant programming techniques to solve


Leetcode Problem No.: 283
Language: Java

Given an integer array nums, move all 0's to the end of it while maintaining the relative order of the non-zero elements.
Note that you must do this in-place without making a copy of the array.

Example 1:

Input: nums = [0,1,0,3,12]
Output: [1,3,12,0,0]
Example 2:

Input: nums = [0]
Output: [0]

Source Code:
class Solution {
    public void moveZeroes(int[] nums) {
        int l = 0;
        int h = 0;
        while (h<=nums.length-1){
            if(nums[h]!=0){
                int temp = nums[l];
                nums[l] = nums[h];
                nums[h] = temp;
                l++;
            }
            h++;
        }
    }
}

Issues Faced:
Took few times until the approach to solve the problem was changed
